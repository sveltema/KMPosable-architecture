rootProject.name = "KMPosable-architecture"
include(":kmposable-core")
include(":kmposable")

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
